# 파일럿 W0 피드백 — 김도은 1차 회의 (2026-06-24)

> 양식: `specs/000-backlog/pilot-feedback-card-template.md` · 상위: `docs/beta-pilot-yemian.md`
> 출처: 2026-06-24 1차 회의(진행 송정현 / 대상 김도은) 전사록 + 피드백 메모.
> 각 항목은 **현재 코드와 대조**해 상태·난이도·분류를 매겼다. (다온, 시니어 개발 검토)

## 주간 요약 (회의 말미)
- **이번 주 마찰 Top 3**: ① 녹음 중 이탈 시 파일 유실 우려  ② 환자 노출 화면에 타 기록 노출  ③ 확정 카드 편집 접근성
- **다음 회의(일요일) 전 배포**: ✅ **C-03 확정 카드 인라인 편집** · ✅ **C-02 환자 대면 보호막** (이 세션)
- **정량 스냅샷**: 회의=시연·세팅 중심, 실사용 계측 1주차부터.

---

## C-01 · 녹음 중 상담창 이탈/종료 시 파일 처리

- **출처**: 김도은 회의 / 메모
- **무엇이 불편했나**: "녹음 시작했다가 바로 상담창을 버리거나 끌 때 녹음 파일이 어떻게 처리되는지 확인 필요."
- **관찰/코드 대조**:
  - 보드(overlay)만 닫으면 녹음은 **계속 유지**됨 — `chair-provider.tsx` 가 chairId별 MediaRecorder·chunks 보관(+ wake lock). **안전.**
  - 그러나 **저장 전 탭 종료/브라우저 닫기** → blob이 메모리에서 사라짐. `beforeunload` 경고 **없음**, 자동 임시저장 **없음**. → **유실 가능.**
  - 빈 녹음(1KB 미만)은 자동 폐기 + 에러 안내(기존 진단 계측).
- **빈도/영향**: 가끔 · 영향: **기록 누락**(치명)
- **개선안(가설)**: ① 녹음 중 `beforeunload` 경고  ② 녹음 blob을 IndexedDB에 주기 영속화 → 새로고침/복귀 시 복구.
- **분류**: [ ] 즉시  [x] **spec 승격**(009-audio-archive 연계)  [ ] 범위 밖
- **처리**: 담당 다온 / 상태 **대기** / 연결 spec: 009

## C-02 · 환자 노출 화면에 타인 기록이 보이면 안 됨 ✅ 배포

- **출처**: 김도은 회의 / 메모
- **무엇이 불편했나**: 환자 대면 화면("환자가 보는 화면에 띄워놓고 시작")에 다른 사람 기록이 노출되면 안 됨.
- **관찰/코드 대조**:
  - 환자 상세 페이지는 `getConsultationsByPatientId` 가 `patient_id`+`institution_id` 필터 → **해당 환자 기록만**. 안전.
  - 그러나 **홈(`app/(dashboard)/page.tsx`)은 기관 전체 미연결 기록을 통합 노출**(`HomeFeed`) + 환자 검색(`PatientHome`). 환자 앞에서 홈을 띄우면 타 환자 정보 노출 위험.
  - 화면 자동잠금(§4)만으론 부족.
- **빈도/영향**: 자주 · 영향: **개인정보 노출**(헌법 I)
- **개선안(적용)**: **환자 대면 보호막**(`PatientShield`). 홈의 `HomeFeed`·`PatientHome`을 기본 가림(흐림+접힘), "기록 펴기" 또는 흐린 영역 클릭 시 노출. 보호 우선이라 펼친 상태는 기억하지 않고 매 진입 시 재가림. `ConsultHero`(상담 시작·PII 없음)는 노출 유지. 큰 안내문이 환자 대면 안내 역할 겸함.
- **분류**: [x] **즉시 배포**  [ ] spec  [ ] 범위 밖
- **처리**: 담당 다온 / 상태 **배포** / 연결: `components/home/patient-shield.tsx`, `app/(dashboard)/page.tsx`

## C-03 · 연결 확정 상담카드도 편집·전체복사 바로 접근 ✅ 배포

- **출처**: 김도은 회의 / 메모
- **무엇이 불편했나**: "연결 환자 상담카드도 '편집'·'전체복사' 바로 접근 가능하도록."
- **관찰/코드 대조**:
  - **전체복사**: 확정 카드 본문 아래 `CopyAllButton` **이미 존재**(`consultation-history.tsx`). 추가 불필요.
  - **편집**: 확정 상담은 `updateDraftConsultation`의 `status='draft'` 가드로 **편집 막혀 있었음**.
- **개선안(적용)**: status 가드 없는 `updateConsultationContent` 액션 추가 + 확정 카드에 인라인 '편집'(`ConfirmedEditControls`). 기관 격리·revalidate 유지.
- **빈도/영향**: 매번 · 영향: **시간 낭비** (STT 오인식 "김동원" 등 사후 정정 빠르게)
- **분류**: [x] **즉시 배포**  [ ] spec  [ ] 범위 밖
- **처리**: 담당 다온 / 상태 **배포** / 연결: `app/actions/consultations.ts` `updateConsultationContent`, `components/consultation-history.tsx` `ConfirmedEditControls`

## C-04 · 새로고침 없이 알림 + 목록 자동 리프레시

- **출처**: 김도은 회의 / 메모
- **무엇이 불편했나**: "새 상담카드 등록되었다 알림 + 목록 리프레시 자동으로."
- **관찰/코드 대조**: **이미 구현됨**(spec 007 P1·P2). `chair_audit_logs` 실시간 구독 → 토스트 + `router.refresh()`(400ms 디바운스) + 소리. PII 없는 감사로그만 전송선에.
- **개선안**: 신규 개발 아님 → **도은이 기기에서 동작 검증** + **🔔 소리 켜기 안내**(armSound 미클릭 시 무음).
- **빈도/영향**: — · 영향: (검증 항목)
- **분류**: [x] **검증/온보딩**  [ ] spec  [ ] 범위 밖
- **처리**: 담당 다온 / 상태 **검증 대기** / 연결 spec: 007

## C-05 · 다른 체어에서 실시간 검토·협업 수정

- **출처**: 김도은 회의 / 메모
- **무엇이 불편했나**: "실시간으로 상담 내용 다른 체어에서 검토 및 협업 수정 가능하도록." (전사: "체어에서 작성→데스크에서 받아 정리")
- **관찰/코드 대조**: **미구현.** Supabase presence/broadcast/CRDT 흔적 0. 현재는 저장 후 순차 편집만.
- **개선안(단계적)**:
  1. (싸게) 실시간 알림에 "N번 체어 상담 시작됨" → 클릭 시 그 보드로 점프(기존 구독 재활용)
  2. (중간) 주기적 자동저장 → 데스크에서 거의 실시간 이어받아 편집 ← 송정현 "바통 받아 편집" = 이 단계
  3. (큰 건) 진짜 동시편집·커서 공유 (CRDT)
- **빈도/영향**: — · 영향: 워크플로 (비전)
- **분류**: [ ] 즉시  [x] **spec 승격**(1→2→3 분할)  [ ] 범위 밖
- **처리**: 담당 다온 / 상태 **대기**

---

## 전사록에서 추가로 건진 것 (메모엔 없음)

| ID | 내용 | 분류 | 비고 |
|---|---|---|---|
| **C-06** | STT 이름 오인식("김동훈→김동원") 본문 오기재 | 정정 UX | 환자 연결은 본문과 분리돼 식별 자체는 안전. 본문은 편집 의존(C-03와 연결) |
| **C-07** | 참여자 선택 거슬림("김도우 어디 있어") | 즉시(소) | 참여자 검색 매끄럽게 |
| **C-08** | 파노라마/이미지 캡처 워크플로 번거로움 | spec 승격 | 도은 반박: 덴탈잡으로 이미 파노라마 문자전송 가능 → 화면 내 캡처도구 상시노출·영역선택·보드삽입 매끄러워야 차별점 |
| **C-09** | 저음량 화자(진 원장) 녹음 누락 우려 | 하드웨어/미래 | 마이크 배치·착용형 테스트로 확인 |
| **C-10** | 다중 체어 음성 겹침(2·3층) | 미래 단계 | 동시 녹음 시 |
| **C-11** | 환자 포털 가입 여정 중복감("가입하고 또 가입") | spec(후순위) | 이번 주 환자 전송 비활성이라 후순위 |
